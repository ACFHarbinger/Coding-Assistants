---
- name: Ensure app directory exists
  ansible.builtin.file:
    path: /opt/coding-assistants
    state: directory
    mode: "0755"

- name: Template environment file
  ansible.builtin.template:
    src: env.j2
    dest: /opt/coding-assistants/.env
    mode: "0600"
