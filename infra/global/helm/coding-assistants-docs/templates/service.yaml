apiVersion: v1
kind: Service
metadata:
  name: {{ include "coding-assistants-docs.name" . }}
  labels:
    {{- include "coding-assistants-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "coding-assistants-docs.labels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
