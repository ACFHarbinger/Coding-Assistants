[package]
name = "claude"
version = "0.1.0"
edition = "2021"
description = "C14.3: opt-in Claude Code Channel MCP bridge for Coding-Assistants"
license = "AGPL-3.0-only"

[[bin]]
name = "coding-assistants-claude-channel"
path = "src/main.rs"

[dependencies]
hub = { path = "../hub" }
mcp-core = { path = "../mcp-core" }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
