[package]
name = "mcp-krita"
version = "0.1.0"
edition = "2021"
description = "MCP bridge to a running Krita instance (via the Coding-Assistants Krita plugin)"
license = "AGPL-3.0-only"

[[bin]]
name = "coding-assistants-mcp-krita"
path = "src/main.rs"

[dependencies]
mcp-core = { path = "../mcp-core" }
serde_json = "1"
