[package]
name = "mcp-blender"
version = "0.1.0"
edition = "2021"
description = "MCP bridge to a running Blender instance (via the Coding-Assistants Blender addon)"
license = "AGPL-3.0-only"

[[bin]]
name = "coding-assistants-mcp-blender"
path = "src/main.rs"

[dependencies]
mcp-core = { path = "../mcp-core" }
serde_json = "1"

[dev-dependencies]
# (mock TCP peer uses std only)
