[package]
name = "mcp-echo"
version = "0.1.0"
edition = "2021"
description = "Minimal reference MCP server on mcp-core — the template each crates/mcp-<tool> copies"
license = "AGPL-3.0-only"

[[bin]]
name = "coding-assistants-mcp-echo"
path = "src/main.rs"

[dependencies]
mcp-core = { path = "../mcp-core" }
serde_json = "1"
