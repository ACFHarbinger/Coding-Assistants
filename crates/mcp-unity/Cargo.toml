[package]
name = "mcp-unity"
version = "0.1.0"
edition = "2021"
description = "MCP bridge to a running Unity editor (via the Coding-Assistants Editor package)"
license = "AGPL-3.0-only"

[[bin]]
name = "coding-assistants-mcp-unity"
path = "src/main.rs"

[dependencies]
mcp-core = { path = "../mcp-core" }
serde_json = "1"
