//! Desktop IPC commands for the durable shared hub.

// The facade file is intentionally named `commands.rs` to match the rest
// of the `commands/` subtree's file-per-concern naming; the containing
// module is also `commands` by directory convention, hence the inception.
#[allow(clippy::module_inception)]
pub mod commands;
